<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- HEADER -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:paddingVertical="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/back" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:fontFamily="@font/bricolage_grotesque_medium"
                android:text="@string/tambah_eventmu"
                android:textColor="@color/hijau3"
                android:textSize="20sp" />
        </LinearLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- CONTENT -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/my_toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp">

            <!-- Image Preview -->
            <ImageView
                android:id="@+id/image_preview"
                android:layout_width="150dp"
                android:layout_height="130dp"
                android:layout_gravity="center"
                android:layout_marginTop="16dp"
                android:background="#DDDDDD"
                android:src="@android:drawable/ic_menu_gallery" />

            <!-- Choose Image -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="12dp">

                <Button
                    android:id="@+id/button_choose_image"
                    android:layout_width="48dp"
                    android:layout_height="32dp"
                    android:layout_marginStart="12dp"
                    android:layout_gravity="center"
                    android:padding="2dp"
                    android:text="+"
                    android:textSize="15sp"
                    android:textColor="@color/white"
                    android:background="@drawable/btn_bg1"
                    app:backgroundTint="@null" />
            </LinearLayout>

            <!-- FORM -->
            <TextView style="@style/Label" android:text="Nama Event"/>
            <EditText style="@style/Input" android:hint="Masukkan Nama Event"
                android:id="@+id/inputNama"/>

            <TextView style="@style/Label" android:text="Penyelenggara"/>
            <EditText style="@style/Input" android:hint="Masukkan Penyelenggara"
                android:id="@+id/inputPenyelenggara"/>

            <TextView style="@style/Label" android:text="Email"/>
            <EditText style="@style/Input"
                android:inputType="textEmailAddress"
                android:hint="Masukkan Email"
                android:id="@+id/inputEmail"/>

            <TextView style="@style/Label" android:text="No. HP"/>
            <EditText style="@style/Input"
                android:inputType="phone"
                android:hint="Masukkan No. HP"
                android:id="@+id/inputNo"/>

            <TextView style="@style/Label" android:text="Deskripsi"/>
            <EditText
                style="@style/Input"
                android:minHeight="120dp"
                android:gravity="top"
                android:inputType="textMultiLine"
                android:id="@+id/inputDesc"/>

            <Button
                android:id="@+id/btnNext"
                android:layout_width="220dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="24dp"
                android:text="Selanjutnya"
                android:textSize="16sp"
                android:background="@drawable/btn_bg1"
                app:backgroundTint="@null"/>
        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
