<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- HEADER -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:paddingVertical="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/back" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:fontFamily="@font/bricolage_grotesque_medium"
                android:text="@string/tambah_eventmu"
                android:textColor="@color/hijau3"
                android:textSize="20sp" />
        </LinearLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- CONTENT -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/my_toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="24dp">

            <!-- Target Dana -->
            <TextView style="@style/Label" android:text="Target Dana"/>
            <EditText
                style="@style/Input"
                android:id="@+id/inputDana"
                android:hint="Masukkan Target Dana"
                android:inputType="number"/>

            <!-- Kuota Relawan -->
            <TextView style="@style/Label" android:text="Kuota Relawan"/>
            <EditText
                style="@style/Input"
                android:id="@+id/inputRelawan"
                android:hint="Masukkan Jumlah Relawan"
                android:inputType="number"/>

            <!-- Lokasi -->
            <TextView style="@style/Label" android:text="Lokasi Proyek"/>
            <EditText
                style="@style/Input"
                android:id="@+id/inputLokasi"
                android:hint="Masukkan Lokasi"/>

            <!-- Tanggal Mulai -->
            <TextView style="@style/Label" android:text="Tanggal Mulai"/>
            <EditText
                style="@style/Input"
                android:id="@+id/inputTgl"
                android:hint="Masukkan Tanggal Mulai"/>


            <!-- Tanggal Selesai -->
            <TextView style="@style/Label" android:text="Tanggal Selesai"/>
            <EditText
                style="@style/Input"
                android:id="@+id/inputSelesai"
                android:hint="Masukkan Tanggal Selesai"/>



            <!-- SUBMIT -->
            <Button
                android:id="@+id/btnPost"
                android:layout_width="220dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="24dp"
                android:text="Posting"
                android:textSize="16sp"
                android:background="@drawable/btn_bg1"
                app:backgroundTint="@null"/>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
